---
const {
  gap,
  size,
  href,
  variant,
  onlyIconSize,
  class: className,
  ...rest
} = Astro.props;
// Default button
const defaultClass = [
  "bg-base-900",
  "text-white",
  "focus:ring-black",
  "hover:bg-base-800",
  "focus:ring-base-800",
];
// Accent button
const accentClass = [
  "text-white",
  "bg-accent-500",
  "hover:bg-accent-600",
  "focus:ring-accent-600",
];
// Muted button
const mutedClass = [
  "text-accent-500",
  "bg-accent-50",
  "hover:bg-accent-200",
  "hover:text-accent-600",
  "focus:ring-accent-200",
];
// Size (only applies if variant is not "link")
const xs = ["h-8", "px-4", "py-2", "text-xs", "rounded-md"];
const sm = ["h-9", "px-4", "py-2", "text-sm", "rounded-md"];
const base = ["h-10", "px-6", "py-3", "text-base", "rounded-lg"];
const md = ["h-11", "px-6", "py-3", "text-md", "rounded-lg"];
const lg = ["h-12", "px-6", "py-3", "text-lg", "rounded-lg"];
const xl = ["h-14", "px-6", "py-3", "text-xl", "rounded-lg"];
// Gap
const gapXS = ["gap-2"];
const gapSM = ["gap-4"];
const gapBase = ["gap-8"];
const gapMD = ["gap-10"];
const gapLG = ["gap-12"];
const additionalClasses = className ? className.split(" ") : [];
---

<a
  href={href}
  class:list={[
    ...(variant === "link"
      ? [...additionalClasses] // Apply custom classes for "link" variant
      : [
          "flex",
          "transition",
          "text-center",
          "justify-center",
          "items-center",
          "duration-300",
          "focus:ring-2",
          "focus:outline-none",
          "focus:ring-offset-2",
          variant === "muted" && mutedClass,
          variant === "accent" && accentClass,
          variant === "default" && defaultClass,
          size === "xs" && xs,
          size === "sm" && sm,
          size === "base" && base,
          size === "md" && md,
          size === "lg" && lg,
          size === "xl" && xl,
          gap === "xs" && gapXS,
          gap === "sm" && gapSM,
          gap === "base" && gapBase,
          gap === "md" && gapMD,
          gap === "lg" && gapLG,
          ...additionalClasses,
        ]),
  ]}
  {...rest}
>
  <slot name="left-icon" />
  <slot />
  <slot name="right-icon" />
</a>
